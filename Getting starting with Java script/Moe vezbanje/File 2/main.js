
/* Arrays are type of object, which
allows you ti store multiple values. 
These values can be of all data types
and one array can eve contain different
data types. */

arr1 = [`purple`, `orange`, `green`];
console.log(arr1);

// Array with 10 undefined values
arr2 = new Array(10);
console.log(arr2);

// Array with one value of 10
arr3 = [10];
console.log([10]);


